/* @license GPL-2.0-or-later https://www.drupal.org/licensing/faq */
(($,Drupal,window,{tabbable},debounce)=>{Drupal.AiToolsLibrary={currentSelection:[]};Drupal.AjaxCommands.prototype.updateAiToolsLibrarySelection=function(ajax,response,status){Object.values(response.aiTools).forEach((value)=>{Drupal.AiToolsLibrary.currentSelection.push(value);});};Drupal.behaviors.AiToolsLibraryTabs={attach(context){const $menu=$('.js-ai-tools-library-menu');$(once('ai-tools-library-menu-item',$menu.find('a'))).on('keypress',(e)=>{if(e.which===32){e.preventDefault();e.stopPropagation();$(e.currentTarget).trigger('click');}}).on('click',(e)=>{e.preventDefault();e.stopPropagation();const ajaxObject=Drupal.ajax({wrapper:'ai-tools-library-content',url:e.currentTarget.href,dialogType:'ajax',progress:{type:'fullscreen',message:Drupal.t('Processing...')}});ajaxObject.success=function(response,status){return Promise.resolve(Drupal.Ajax.prototype.success.call(ajaxObject,response,status)).then(()=>{const aiToolsLibraryContent=document.getElementById('ai-tools-library-content');if(aiToolsLibraryContent){const tabbableContent=tabbable(aiToolsLibraryContent);if(tabbableContent.length)tabbableContent[0].focus();}});};ajaxObject.execute();$menu.find('.active-tab').remove();$menu.find('a').removeClass('active');$(e.currentTarget).addClass('active').html(Drupal.t('<span class="visually-hidden">Show </span>@title<span class="visually-hidden"> tool</span><span class="active-tab visually-hidden"> (selected)</span>',{'@title':$(e.currentTarget).data('title')}));Drupal.announce(Drupal.t('Showing @title tool.',{'@title':$(e.currentTarget).data('title')}));});}};Drupal.behaviors.AiToolsLibraryItemSelection={attach(context,settings){const $form=$('.ai-tools-library-view__rows',context);const currentSelection=Drupal.AiToolsLibrary.currentSelection;const aiToolsLibraryModalSelection=document.querySelector('#ai-tools-library-modal-selection');if(aiToolsLibraryModalSelection){aiToolsLibraryModalSelection.value=currentSelection.join();$(aiToolsLibraryModalSelection).trigger('change');}if(!$form.length)return;const $aiTools=$('.ai-tools-library-item input[type="checkbox"]',$form);$(once('ai-tools-item-change',$aiTools)).on('change',(e)=>{const id=e.currentTarget.value;if(e.currentTarget.checked){if(!currentSelection.includes(id))currentSelection.push(id);}else{if(currentSelection.includes(id))currentSelection.splice(currentSelection.indexOf(id),1);}const aiToolsLibraryModalSelection=document.querySelector('#ai-tools-library-modal-selection');if(aiToolsLibraryModalSelection){aiToolsLibraryModalSelection.value=currentSelection.join();$(aiToolsLibraryModalSelection).trigger('change');}});currentSelection.forEach((value)=>{$form.find(`input[type="checkbox"][value="${value}"]`).prop('checked',true).trigger('change');});}};Drupal.behaviors.AiToolsLibraryModalClearSelection={attach(){if(!once('ai-tools-library-clear-selection','html').length)return;window.addEventListener('dialog:afterclose',()=>{Drupal.AiToolsLibrary.currentSelection.length=0;});}};Drupal.behaviors.AiToolsLibraryFilter={attach(context,settings){const [input]=once('tools-filter-text','input.tools-filter');if(!input)return;const $toolsWrapper=$('.ai-tools-selection');const $tools=$toolsWrapper.find('.ai-tools-library-item');function preventEnterKey(event){if(event.which===13){event.preventDefault();event.stopPropagation();}}function filterToolsList(e){const query=e.target.value;if(query.length===0)$tools.show();const re=new RegExp(`\\b${query}`,'i');function showTool(index,row){const sources=row.querySelectorAll('.option');if(sources.length>0){const textMatch=sources[0].textContent.search(re)!==-1;$(row).toggle(textMatch);}}$tools.show();if(query.length>=2){$tools.each(showTool);const visibleTools=$toolsWrapper.find('.ai-tools-library-item:visible');Drupal.announce(Drupal.formatPlural(visibleTools.length,'1 tool is available in the modified list.','@count tools are available in the modified list.'));}}$(input).on({keyup:debounce(filterToolsList,200),click:debounce(filterToolsList,200),keydown:preventEnterKey});}};})(jQuery,Drupal,window,window.tabbable,Drupal.debounce);;
